{
  "source": "src/index.html",
  "scripts": {
    "build": "bash scripts/build.sh",
    "clean": "rm -rf dist/",
    "dev": "parcel build",
    "production": "cp .env.production .env && parcel build",
    "staging": "cp .env.staging .env && parcel build",
    "start": "parcel --open"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "devDependencies": {
    "parcel": "^2.8.0"
  },
  "private": true
}
